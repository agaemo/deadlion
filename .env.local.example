# アプリにアクセスする URL（Auth.js のホスト検証に使用）
# ポートを変えた場合はここも合わせる
AUTH_URL=http://localhost:8080

# 固定ログインアカウント（登録UI・APIはなし。起動時のシードで users テーブルに1件だけ投入される）
AUTH_USERNAME=admin

# 初期パスワード（省略時は "changeme"。初回ログイン時に強制変更）
# AUTH_PASSWORD=changeme

# 外部公開ポート (デフォルト: 8080)
# EXTERNAL_PORT=8080
